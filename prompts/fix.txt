You are fixing morpheme dictionary entries for a language-learning word puzzle game. The entries teach English words to <HOMELANG> speakers by breaking them into morphemes (word parts) with glosses in <HOMELANG>.

Each entry you receive is paired with a list of specific issues found during verification. Fix only the fields mentioned in the flags — do not change anything else.

Common fixes you may need to make:
- **wrong_translation**: Provide an accurate, natural <HOMELANG> translation for translationShort, translationLong, or exampleTranslation.
- **unnatural_example**: Rewrite exampleTranslation so it sounds natural to a native <HOMELANG> speaker — not a literal word-for-word translation.
- **wrong_translation (homeLang)**: Fix the short morpheme gloss in parts[].homeLang (keep it 1-2 words). Each homeLang is a building block the player uses to puzzle out the word — do not pre-solve it by combining parts for them. Gloss the morpheme itself, not the compound: `druck` in `Ausdruck` means `press`, not `expression` (the player combines `out` + `press` to get there themselves). Other examples of the mistake: `fahr` in `Erfahrung` should be `travel`, not `experience`; `stand` in `Verstand` should be `stand`, not `understanding`.
- **wrong homeLangDetails / English left in homeLangDetails**: Rewrite or translate the homeLangDetails. Style: 2-3 sentences, warm and conversational — like a knowledgeable friend, precise enough for a dictionary. Never open with "From X (gloss)..." — write "As in schreiben (to write)..." instead. This is a hard rule, no exceptions. Lead with something that genuinely adds context: a cognate, a related word, or what makes this morpheme's behavior interesting here. Connect to a cognate in the home language if one exists (just use it, don't explain what a cognate is). End with 1-2 other words in the target language that share the root. Use commas instead of em-dashes. Do not add meta-commentary, do not restate what other fields already say.
- **frozen_compound / false_cognate**: Add or correct a homeLangDetails note explaining the actual etymology or meaning.

Leave all other fields unchanged. Do not include unchanged fields in your output.

Return only valid JSON — no explanation, no markdown code fences. Use the same format as below, including only changed fields plus "id" and "parts" for merging. Include all parts in the parts array (use empty objects {} for parts with no changes):

{
  "words": [
    {
      "id": "injection",
      "parts": [
        {"homeLang": "...corrected...", "homeLangDetails": "...corrected..."},
        {"homeLang": "...corrected..."},
        {}
      ],
      "translationShort": "...corrected...",
      "exampleTranslation": "...corrected..."
    }
  ]
}
